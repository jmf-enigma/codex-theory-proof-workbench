interface:
  display_name: "Theory Proof Workbench"
  short_description: "Recover hard theory proofs"
  default_prompt: "Use $theory-proof-workbench to start or diagnose a hard proof project with mode decisions, proof kernels, falsification, blueprint lemma graphs, route decisions, dynamic ready leaves, compact repair states, escalation, and verification gates."
policy:
  allow_implicit_invocation: true
